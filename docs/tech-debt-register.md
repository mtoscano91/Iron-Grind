## Technical Debt Register
Last updated: 2026-07-09
Total items: 9 | Estimated total effort: S + S + S + S + S + M + M + S + S

| ID | Category | Description | Files | Effort | Impact | Priority | Added | Sprint |
|----|----------|-------------|-------|--------|--------|----------|-------|--------|
| TD-008 | Test Debt | `WireFixedPointCodec.EncodeCritChance`/`EncodeAttackSpeedMultiplier` have no overflow guard — an out-of-range input silently wraps via the `ushort` cast rather than clamping+logging like the other four CR-NET-7.2 encoders (position, rotation, direction, cycleTimer). CR-NET-7.2 documents a valid range for these two fields but Story 003's Implementation Notes only specify a runtime guard for the other four; accepted as a deliberate scope-discipline call, not an oversight. | `src/Foundation/Networking/WireProtocol/WireFixedPointCodec.cs` | S | Low | — | 2026-07-09 | Backlog |
| TD-009 | Correctness Debt | None of `EncodePosition`/`EncodeRotation`/`EncodeDirection` guard against `NaN`/`Infinity` input — `Mathf.Clamp(NaN, ...)` returns `NaN` (the clamp body never executes since IEEE754 NaN comparisons are always false), and the subsequent `Mathf.RoundToInt` + `(short)` cast of `NaN` is not guaranteed to behave identically between the Mono Editor test run and an IL2CPP ARM64 client build. Flagged by unity-specialist code review; no AC requires NaN handling so not fixed in Story 003. | `src/Foundation/Networking/WireProtocol/WireFixedPointCodec.cs` | S | Medium | — | 2026-07-09 | Backlog |
| TD-001 | Architecture Debt | `ItemDatabase.GetItemsByCategory` returns the internal `List<ItemDefinition>` upcast to `IReadOnlyList<ItemDefinition>` rather than a defensive wrapper. A caller casting back to `List<T>` could mutate the database's internal index, undermining the "single authoritative data" guarantee. Accepted for Story 001 because it requires a deliberate invalid cast to trigger — no accidental-misuse path exists today. | `src/Foundation/ItemDatabase/ItemDatabase.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-002 | Dependency Debt | **RESOLVED 2026-07-04**: `src/` and `tests/` are now local Unity packages (`com.irongrind.src`, `com.irongrind.tests`, both `file:` references in `Packages/manifest.json`), each with their own asmdef, `InternalsVisibleTo` wiring test seams across the assembly boundary, and `"testables"` registration so the Test Runner discovers package tests. Confirmed working: all 128 EditMode tests across Character Stats (Stories 001–007) and Item Database (Stories 001–003) compile and pass in the real Editor. | `src/package.json`, `tests/package.json`, `src/Foundation/IronGrind.Foundation.asmdef`, `src/Foundation/AssemblyInfo.cs`, `tests/EditMode/IronGrind.Foundation.EditModeTests.asmdef`, `Packages/manifest.json` | — | — | — | 2026-07-04 | Closed |
| TD-003 | Test Debt | Two documented `ItemDatabase.Initialize()` behaviors have no regression test: duplicate `ItemID` across records is last-write-wins (no error logged), and `null` entries in the records enumerable are silently skipped. Neither is required by a story AC, but both are contractual behavior that a future refactor could silently change. Accepted for Story 001 because the 11 blocking ACs are fully covered; these are defensive-behavior gaps only. | `src/Foundation/ItemDatabase/ItemDatabase.cs`, `tests/EditMode/ItemDatabase/ItemDatabase_Core_tests.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-004 | Documentation Debt | `ItemDefinitionValidator`'s class remarks justify using a `HashSet<StatID>` (instead of `Enum.IsDefined`) as if `StatID` carries a unique IL2CPP stripping risk that `GearSlot` (validated via `Enum.IsDefined` two lines away) does not. Per the Unity specialist review, both enums are equally protected by their `[SerializeField]` usage elsewhere — the real justification is boxing-avoidance in general, not an asymmetric risk between the two enums. Accepted for Story 002 because the code itself is correct either way; only the comment's reasoning is imprecise. | `src/Foundation/ItemDatabase/ItemDefinitionValidator.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-005 | Test Debt | Two validator code paths have no test coverage: the undefined-`ItemCategory` default-case error (`ValidateRecord`'s `switch` default branch), and the Equipment-record-with-`ConsumableData`-set cross-contamination check in `ValidateEquipment`. Neither is required by a story-002 AC (they're defensive paths the validator happens to also cover), but both are real, reachable code paths that a future refactor could silently break. Accepted for Story 002 because all 15 blocking ACs are fully covered by the 25 existing tests. | `src/Foundation/ItemDatabase/ItemDefinitionValidator.cs`, `tests/EditMode/ItemDatabase/ItemDatabase_Validator_Error_tests.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-006 | Dependency Debt | **RESOLVED 2026-07-04**: a real Unity 6.3 LTS (6000.3.10f1) Editor project exists at the repo root; `src`/`tests` compile as local packages (TD-002); all 128 EditMode tests across Stories 001–007 (Character Stats) and 001–003 (Item Database) actually pass in the Editor — the first real execution since these stories were written. Four genuine pre-existing bugs were found and fixed along the way (all undetected until this session because nothing had ever compiled before): (1) `StatChangedHandler`/`EntityDiedHandler` used bare instead of qualified — they're nested types inside the `CharacterStats` class, not namespace-level; (2) `Object.DestroyImmediate` ambiguous between `System.Object`/`UnityEngine.Object` in 4 test files; (3) `CharacterStats.StatSlotCount` bare reference misresolved because the test namespace itself ends in "...CharacterStats", colliding with the class name; (4) `(ItemCategory)999` — compile-time constant overflow on a byte-backed enum (CS0221), inherited from the story's own illustrative AC-19 text. **Still open**: Story 004's `ItemDatabaseSeeder` menu tool has not been run yet — the 34 real `.asset` files and smoke-check evidence are still pending. | — | — | — | — | 2026-07-04 | Closed (Story 004 asset generation tracked separately, not blocking) |
| TD-007 | Documentation Debt | ~40 files across `design/gdd/`, `design/registry/entities.yaml`, and other project docs still reference lowercase `assets/...` paths for future (not-yet-authored) asset locations (e.g. `assets/data/SkillSystemConfig.asset` in `entities.yaml`). Now that the real folder is `Assets/` (capitalized), these will be wrong the moment those systems are actually implemented. Not fixed now because the referenced assets don't exist yet and the blast radius (~40 files) is large relative to the value of fixing paths with no real target yet. Fix each reference when its owning system is actually implemented, not in bulk. (Excludes `.claude/` framework files, skill/hook templates, and the `CCGS Skill Testing Framework/` sandbox, which correctly use `assets/` as an engine-agnostic generic term, not a literal path into this project.) | `design/gdd/*.md`, `design/registry/entities.yaml` (~40 files) | M | Low | — | 2026-07-04 | Backlog — fix opportunistically per-system, not in bulk |

### Rules
- Tech debt is not inherently bad — it is a tool. The register tracks conscious decisions.
- Every debt entry must explain WHY it was accepted (deadline, prototype, missing info)
- "Scan" should run at least once per sprint to catch new debt
- Items older than 3 sprints without action should either be fixed or consciously accepted with a documented reason
