## Technical Debt Register
Last updated: 2026-07-22
Total items: 33 | Estimated total effort: S + S + S + S + S + S + S + S + S + S + S + S + S + S + M + M + S + S + S + S + S + S + M + S + S + S + S + L + M + S + S + S + S

| ID | Category | Description | Files | Effort | Impact | Priority | Added | Sprint |
|----|----------|-------------|-------|--------|--------|----------|-------|--------|
| TD-035 | Test Debt | `TestOnly_ThrowDuringRespecStep2`/`TestOnly_ThrowAfterRecomputeDerivedStats` (`LevelingService.cs`, Leveling System Story 007) introduce a `TestOnly_`-prefixed underscore-in-identifier naming style — a first-of-its-kind pattern in this codebase (every other test-only seam, e.g. `TransportFaultInjector`/`ITransportFaultInjector`, is a whole guarded file/interface, not an inline field with this naming shape). Harmless as a one-off, but if this idiom recurs in future stories it should be codified in `.claude/docs/coding-standards.md` rather than re-decided ad hoc per story. No action needed unless/until a third instance appears. | `src/Foundation/LevelingSystem/LevelingService.cs`, `.claude/docs/coding-standards.md` | S | Low | — | 2026-08-15 | Backlog |
| TD-034 | Architecture Debt | `RespecTwoPhaseCommitCoordinator.ExecuteRespec` (Leveling System Story 007) calls `reservation.Consume()` after its `try` block, outside exception handling — correct per CR-4.1 (only `TryApplyRespec` can legitimately fail in the two-phase-commit sense). But `IItemReservation` has no documented no-throw contract on `Consume()`/`Release()`; today's test double never throws, so this is unreachable in practice. Once a real Inventory System implements `IItemReservation`, either document that `Consume()`/`Release()` must not throw, or add explicit handling for that case. Flagged by unity-specialist review, not applied this story (correctly deferred — no real implementation exists yet to harden against). | `src/Foundation/LevelingSystem/RespecTwoPhaseCommitCoordinator.cs`, `src/Foundation/LevelingSystem/IItemReservation.cs` | S | Low | — | 2026-08-15 | Backlog — resolve when a real Inventory System `IItemReservation` implementation lands |
| TD-033 | Spec Conflict | `leveling-system.md`'s AC-LS-43 worked example states the raw (pre-clamp) MaxMP value for INT=420 at tier ×2.0 as `10,168`. Hand-verified during Leveling System Story 004: `(100 + 420×12) × 2.0 = 5140 × 2.0 = 10,280`, not `10,168` — the GDD's own arithmetic appears to be a genuine error. Did not block Story 004 (its test only asserts the post-clamp result, 9999, which is correct either way and doesn't depend on which raw number is right) but the design doc text itself should be corrected for anyone reading it as a reference. | `design/gdd/leveling-system.md` | S | Low | — | 2026-07-22 | Backlog |
| TD-032 | Bug | `CurrencySystem.OnGoldSync` (`src/Foundation/Currency/CurrencySystem.cs:144,275`) fires via a bare `OnGoldSync?.Invoke(...)` — a single throwing subscriber aborts every subsequent subscriber in the invocation list. Violates ADR-010 Decision 3's implicit exception-isolation expectation (the same requirement CR-2.10 states explicitly for `LevelingService.OnLevelUp`, which correctly uses manual `GetInvocationList()` + per-handler try/catch instead). Discovered incidentally during Leveling System Story 003 while researching this codebase's existing broadcast-event precedents — `CurrencySystem` predates that discipline being applied. Not fixed here (different epic, out of scope for Story 003). Fix: replace the bare `?.Invoke()` with the same manual-iteration + try/catch pattern `LevelingService.FireOnLevelUp` already establishes. | `src/Foundation/Currency/CurrencySystem.cs` | S | Low | — | 2026-07-22 | Backlog |
| TD-031 | Spec Conflict | `leveling-system.md` AC-LS-40 requires `AddExperience(entity, amount<=0)` to log an error. The already-Complete, already-closed Character Stats implementation (`CharacterStats.AddExperience`, Story 006) silently returns on that path with no `Debug.LogError`/`LogWarning` call anywhere. Discovered during Leveling System Story 001's `/dev-story` context load — confirmed via full-file grep for logging calls near `AddExperience`. User decision (this session): relax Story 001's AC-LS-40 to drop the log assertion rather than modify already-closed Character Stats code. Two ways to close this: (a) add the missing log call to `CharacterStats.AddExperience` in a future small Character Stats revisit, or (b) correct `leveling-system.md`'s AC-LS-40 text to match the as-built (no-log) behavior. Neither decided yet — flagging both options for whoever picks this up. | `src/Foundation/CharacterStats/CharacterStats.cs`, `design/gdd/leveling-system.md`, `production/epics/leveling-system/story-001-xp-accumulation.md` | S | Low | — | 2026-07-22 | Backlog |
| TD-030 | Test Debt | Story 029's `SetTarget`/`TargetSlotTracker` tests do not exercise the combined "party-set change + `SetTarget` arrive in the same tick" ordering scenario from the story's own Implementation Notes ("party set updates are applied first, then target slot updates... combined relevance state is always consistent at the flush point," RFR-3). Correctly deferred, not a gap in Story 029 itself — no real Party System exists yet to drive a party-membership-change side of that composition (same forward-dependency class as TD-017 through TD-023). Flagged so the future Party System epic's regression checklist picks up this combined-ordering scenario once a real party-membership-change entry point exists to test against. | `production/epics/networking-core/story-029-settarget-rpc-target-slot-management.md`, `tests/EditMode/Networking/RelevanceFilter_SetTargetRpc_tests.cs` | S | Low | — | 2026-07-22 | Backlog — pick up when the Party System epic begins |
| TD-029 | Bug | **RESOLVED 2026-07-21, same session.** First real Unity Editor test run of this session (Story 028) surfaced 5 pre-existing test failures in already-"Complete" stories, undetected until then because nothing had ever compiled/run before. All 5 turned out to be **test bugs, not production bugs** — no production code required any change. (1) **AC-GH-10** (`GhostSession_Cleanup_Crash_Dismissal_tests.cs:129`) and (2) **AC-GH-11** (same file, `ProcessDismissalRequest_..._AC_GH_11`): both asserted `callOrder` *after* appending `"persist"` to it instead of before, so the assertion always saw its own just-added entry as unexpected extra content — fixed by reordering assert-then-append. (3) **AC-GH-7** (`GhostSession_RewardForfeitPolicy_tests.cs:195`): passed `targetingMobIds: Array.Empty<uint>()` while still asserting a `"deTarget"` callback fired — `MobDeTargetingCoordinator.ProcessTTLExpiry` calls the callback once per `targetingMobIds` entry, so an empty array never fires it; fixed by supplying a real mob ID. (4) **AC-GH-14** (`GhostSession_Cleanup_Crash_Dismissal_tests.cs:386`): the test only anticipated `RecordFailedReAuthAttempt`'s own `OnSessionStateTransitioned` call, missing that `EnterReconnecting` (called immediately before it) also legitimately fires one — fixed by expecting both, in order. (5) `TryWriteSnapshot_DifferentDisconnectTickNumber_OverwritesAndReturnsTrue` (`GhostSession_SnapshotWriteOrdering_tests.cs:137`): `Assert.AreEqual((9, 9, 9), stored.RespawnPosition)` — `RespawnPosition` is `(short, short, short)`, but the tuple literal defaults to `(int, int, int)` with no target-typing context inside `Assert.AreEqual`'s `object` parameters; two different `ValueTuple` types with identical `ToString()` output, so the printed failure showed "Expected: (9,9,9), But was: (9,9,9)" despite matching values — fixed with explicit `short` casts. All 5 fixes independently confirmed to leave 725/730 passing (same 5 tests, now passing; no new regressions). | `tests/EditMode/Networking/GhostSession_Cleanup_Crash_Dismissal_tests.cs`, `tests/EditMode/Networking/GhostSession_RewardForfeitPolicy_tests.cs`, `tests/EditMode/Networking/GhostSession_SnapshotWriteOrdering_tests.cs` | M | High | — | 2026-07-21 | Closed |
| TD-028 | Architecture Debt | This project (a live-service MMORPG) has no PlayMode/Integration test tier at all — every one of the 27 Networking Core stories so far, including several individually tagged "(Integration)" in their own acceptance criteria, has been verified via `tests/EditMode/` structural composition (test-harness injection points simulating multi-system interaction deterministically), never a real multi-client PlayMode session. This was surfaced during Story 027's `/story-readiness` check: the story's own Test Evidence section (before correction) demanded `tests/PlayMode/Networking/...` or a documented playtest — the first story in this epic to name that tier at all — contradicting its own header (`Type: Logic`) and QA Test Cases section (EditMode path). Resolved for Story 027 by aligning it with the established EditMode-composition precedent, consistent with every prior story and with the GDD's own AC-MCR-05/08 text (which names `ITransportFaultInjector`/`INetworkTestObserver` as the expected mechanism, not a live session). Not resolved: whether this project should ever build genuine PlayMode/multi-client Integration testing infrastructure (scaffolding, CI job, ADR) before launch — for an MMORPG specifically, EditMode composition tests prove logic correctness in isolation but never exercise real NGO transport wiring, actual packet loss, or genuinely concurrent client sessions. No story or ADR currently owns this decision. **Recurrence**: Story 028's `/story-readiness` check found the identical self-contradiction (header `Type: Logic` + EditMode QA Test Cases vs. a Test Evidence section demanding PlayMode/playtest) — same fix applied. Two occurrences in two consecutive stories suggests this may be a stale pattern baked into whatever template/process generated these stories' Test Evidence sections, not isolated authoring mistakes; worth checking other not-yet-implemented stories in this epic for the same issue before they reach `/story-readiness`. | `production/epics/networking-core/story-027-selfdamageevent-damageevent-exclusivity.md`, `story-028-relevance-filter-algorithm.md` (surfaced here); applies project-wide | L | Medium | — | 2026-07-21 | Backlog — needs a producer/technical-director decision on whether/when to build a PlayMode Integration tier, likely before launch given live-multiplayer risk profile |
| TD-027 | Test Debt | `IZoneTestConfigurator.SetBatchSizeLimit`/`ZoneTestConfigurator.GetBatchSizeLimitOverride` (Story 026, added to satisfy AC-MCR-01/AC-MCR-07's explicit test-harness gate) has zero production call sites — confirmed by grep across `src/`. `RUBatchWriter.MAX_MESSAGE_BODY_BYTES` remains a compile-time structural constant (out of scope for Story 026, per that class's own doc comment) and never reads this override. Same forward-dependency-placeholder pattern as TD-026 (`ConnectionSequenceCounter`) and, on inspection, the same actual status as `IZoneTestConfigurator.SetZoneCapacity` itself — `ZoneSessionStateMachine`'s own doc comment already states it "never reads from or writes to" that override either, and `GetZoneCapacityOverride` likewise has zero production call sites (confirmed by grep). Story 026's own tests cover `SetBatchSizeLimit` directly (set/read-back/instance-scoping/reset), so this entry tracks the production-wiring gap only, not a test-coverage gap. No story currently claims ownership of wiring a real, configurable per-connection R-U batch-size check. | `src/Foundation/Networking/TestHarness/IZoneTestConfigurator.cs`, `src/Foundation/Networking/TestHarness/ZoneTestConfigurator.cs`, `src/Foundation/Networking/WireProtocol/RUBatchWriter.cs` | S | Low | — | 2026-07-21 | Backlog — resolve when a real per-connection R-U send/accumulator layer exists to wire a configurable batch-size limit into |
| TD-026 | Test Debt | `ConnectionSequenceCounter` (Story 025, the CCR-1 per-connection shared-`SequenceNumber`-issuing counter) has zero production call sites — confirmed by grep across `src/`. No live message-send/dispatch layer exists yet in this codebase to construct one instance per real connection and call `IssueNext()` from it. AC-CCR-02 is proven structurally instead (two envelopes issued back-to-back from one shared instance, standing in for `HeartbeatMessage`/`RttProbeEcho` since no `RttProbeEcho` class exists yet either) — same class of forward-dependency gap as TD-020 (`GhostXpPoolTracker.ResolveFinalXp`). No story currently claims ownership of the real per-connection wiring. | `src/Foundation/Networking/WireProtocol/ConnectionSequenceCounter.cs`, `tests/EditMode/Networking/MessageRouting_CriticalityChannelTable_tests.cs` | S | Low | — | 2026-07-21 | Backlog — resolve when a real connection/message-dispatch layer exists to construct and own one instance per connection |
| TD-025 | Test Debt | Story 002's `NetworkingTestHarness_Observer_tests.cs` has a `Reset_ClearsAllRecordedCallsAndOutboundMessageCounts` test whose own doc comment states its purpose is to invoke *every* `INetworkTestObserver` callback so a partial-`.Clear()` regression in `NetworkTestObserver.Reset()` is always catchable. That test was not extended to cover `OnConnectionQualityUpdateEmitted` (added by Story 024) — nor the concurrently-added `OnPartyDisbanded`/`OnCriticalInfrastructureAlertFired` from earlier stories. Production `Reset()` is currently correct and does clear `ConnectionQualityUpdateEmittedCalls` (confirmed), so nothing is broken today — but a future accidental removal of that one `.Clear()` line would go undetected by this regression test. Flagged by unity-specialist code review, Story 024. | `tests/EditMode/Networking/NetworkingTestHarness_Observer_tests.cs`, `src/Foundation/Networking/TestHarness/NetworkTestObserver.cs` | S | Low | — | 2026-07-20 | Backlog — extend the exhaustive-reset test to cover all 3 newer callbacks in one pass |
| TD-024 | Test Debt | AC-OWL-06a/AC-OWL-06c's ghost-period tests (`PlayerSlot_SimulatedReconnectCycle_SlotIndexAndLastBeatTick_ArePreserved`, `PlayerSlot_GhostPeriodBeatsFiringAtIncreasingTicks_ReadsAlwaysReflectMostRecent`, Story 022) simulate the ghost period by simply never calling `DeallocatePlayerSlot` between allocation and a "simulated reconnect." `LastBeatServerTickTracker` has no connection-state, TTL, or ghost-period concept at all — the tests prove "nothing in this class auto-evicts a slot," not the full EC-OWL-2 guarantee that the real (not-yet-built) zone session manager never prematurely calls `DeallocatePlayerSlot` during `Disconnected_SessionActive`. Same accepted forward-dependency limitation class as this epic's other ghost-related composition tests (TD-017 through TD-023). Flagged by qa-tester code review, Story 022. | `tests/EditMode/Networking/OwlCompensation_SlotAllocation_tests.cs`, `src/Foundation/Networking/OwlCompensation/LastBeatServerTickTracker.cs` | S | Low | — | 2026-07-20 | Backlog — resolve when a real zone session manager exists to integration-test against |
| TD-023 | Architecture Debt | `GhostCleanupSequencer.CompleteTTLExpiryCleanup`/`CompleteVoluntaryDismissalCleanup` (Stories 018/021) internally execute CR-GH-10 steps 3 (persist), 6 (session transition), 7 (broadcast) — but steps 4 (remove ghost entity) and 5 (release party-XP tracking) only run afterward, at the *caller's* site (`GhostDismissalCoordinator.ProcessDismissalRequest`, and the equivalent TTL-expiry composition in this story's own tests). The actual executed order is therefore 3, 6, 7, 4, 5 — not the GDD's literal 3, 4, 5, 6, 7 the story's own Control Manifest Rules restate as "Required." No AC assertion currently catches this (neither `GhostEntityTracker.RemoveGhost` nor `GhostXpPoolTracker.EndTracking` fire an observer callback, so no call-order test can see the discrepancy), and practical risk is low given this codebase's fully synchronous, single-threaded tick-loop model — but it originates in already-merged Story 018 code and was carried forward, not introduced fresh, by Story 021. Flagged by unity-specialist code review, Story 021. | `src/Foundation/Networking/GhostSession/GhostCleanupSequencer.cs`, `src/Foundation/Networking/GhostSession/GhostDismissalCoordinator.cs`, `src/Foundation/Networking/GhostSession/GhostEntityTracker.cs`, `src/Foundation/Networking/GhostSession/GhostXpPoolTracker.cs` | S | Low | — | 2026-07-18 | Backlog — resolve if/when a real orchestration layer needs a stricter ordering guarantee than this codebase's current single-threaded execution model already provides |
| TD-022 | Test Debt | AC-GH-10's crash-simulation test (`CompleteTTLExpiryCleanup_SimulatedCrashAfterGhostCleanupPersistenceWrite_...`, Story 021) proves "an exception thrown mid-sequence stops steps 4-7 from running" — not "a crash after persistence write, followed by an actual server restart, recovers the persisted record." No restart/process-recreation/reload-from-disk infrastructure exists anywhere in this codebase for any crash scenario (confirmed). This is a narrower claim than AC-GH-10's own pass-condition text implies ("after crash recovery... exists after server restart"), matching this codebase's own already-logged precedent (TD-019, TD-015) of crash-injection tests proving "value reached the write call" rather than genuine restart-survival. Flagged by qa-tester code review, Story 021. | `tests/EditMode/Networking/GhostSession_Cleanup_Crash_Dismissal_tests.cs`, `src/Foundation/Networking/TestHarness/ServerCrashInjector.cs` | S | Low | — | 2026-07-18 | Backlog — resolve when a real persistence/restart-recovery layer exists to test against |
| TD-021 | Test Debt | AC-GH-16's composition test (`EvaluateJoinAttempt_GhostOccupiesFinalCapacitySlot_NewJoinRejected_AC_GH_16`, Story 021) constructs a genuine ghost session (via both `GhostEntityTracker.PromoteToGhost` and a real `ConnectionStateMachine` heartbeat-timeout transition) but then passes `otherConnectedPlayers + 1` into `ZoneSessionStateMachine.EvaluateJoinAttempt` by hand — no orchestration code anywhere in this codebase sums a zone's live ghost count with its connected-player count to produce a real `currentOccupiedSlots` value. This proves the ghost-creation path works (already tested) and that `EvaluateJoinAttempt(N, N)` rejects (already tested, Story 014) independently, but never proves a real slot-counting code path would actually include the ghost. Forward-dependency gap, not a code defect — no such orchestration layer exists yet. Flagged by qa-tester code review, Story 021. | `tests/EditMode/Networking/GhostSession_Cleanup_Crash_Dismissal_tests.cs`, `src/Foundation/Networking/ZoneSessionStateMachine/ZoneSessionStateMachine.cs` | S | Low | — | 2026-07-18 | Backlog — resolve when a real zone-session orchestration layer computes occupied-slot counts from live registries |
| TD-020 | Test Debt | `GhostXpPoolTracker.ResolveFinalXp` (Story 020's CR-GH-9/9.1/9.2 forfeit-vs-restore decision point) has zero production call sites — confirmed by grep across `src/`. AC-GH-7/AC-GH-12 (TTL expiry, ghost death) compose it inside Story 018's already-tested `GhostCleanupSequencer` `persistCharacterState` delegate, a real extension point, so those two are an acceptable interim proof pending real wiring. AC-GH-8 (reconnect restore) is weaker: `ConnectionStateMachine.CompleteReAuthSuccess` (Story 013) has no delegate seam or any call into `GhostXpPoolTracker` at all, so the test's state-transition and XP-arithmetic assertions are two causally-independent checks, not one integrated pathway — a future reconnect implementation could wire the restore incorrectly (or not at all) and this test would not catch it. No story (checked 021) currently claims ownership of building the real wiring for either path. Flagged by qa-tester and unity-specialist code review, Story 020. | `src/Foundation/Networking/GhostSession/GhostXpPoolTracker.cs`, `src/Foundation/Networking/ConnectionStateMachine/ConnectionStateMachine.cs`, `src/Foundation/Networking/GhostSession/GhostCleanupSequencer.cs`, `tests/EditMode/Networking/GhostSession_RewardForfeitPolicy_tests.cs` | M | Medium | — | 2026-07-18 | Backlog — resolve when a real party-XP-award/persistence-layer story wires these call sites |
| TD-019 | Test Debt | EC-CGS-2's idempotent-write guarantee (`PreDisconnectSnapshotWal.TryWriteSnapshot`, Story 018) exists specifically to survive "a re-submitted identical snapshot after a crash mid-write," but `IServerCrashInjector.CrashStep` has no enum value for the CGS-3 snapshot-WAL write itself — only `AfterGhostCleanupPersistenceWrite` (a different write, at cleanup time) exists. The current idempotency test proves the key-comparison logic in isolation but cannot exercise the actual crash-recovery narrative end-to-end (code review, Story 018, confirmed by qa-tester and independently verified: no matching `CrashStep` member exists). Resolve by adding a `CrashStep.AfterSnapshotWalWrite` (or similar) member and a companion test that registers a crash there, simulates the resubmission, and proves no double-processing — once a real persistence-layer story needs this crash boundary tested for real. | `src/Foundation/Networking/TestHarness/IServerCrashInjector.cs`, `src/Foundation/Networking/GhostSession/PreDisconnectSnapshotWal.cs`, `tests/EditMode/Networking/GhostSession_SnapshotWriteOrdering_tests.cs` | S | Low | — | 2026-07-18 | Backlog — resolve when a real persistence-layer story needs this crash boundary tested |
| TD-018 | Test Debt | AC-GH-2's freeze test (`TenConsecutiveTicks_PositionAndAttackQueueDepth_RemainFrozen`, Story 017) is honestly tautological given current scope: `GhostEntityTracker` has no method anywhere that could mutate a ghost's frozen position or attack-queue depth, so the test cannot currently fail for any reason other than a direct change to `PromoteToGhost`/`TryGetFrozenPosition` themselves — it proves "the mock doesn't move because nothing can move it," not "a real future movement system respects the freeze." Correct and unavoidable given no Combat/Movement system exists yet in this codebase (code review, Story 017, confirmed by both unity-specialist and qa-tester). Needs a companion test once a real "attempt to move a ghost" code path exists (likely a Movement System or Combat System story) that actually attempts to move/queue an attack for a ghosted character and asserts the attempt is rejected/no-ops. | `src/Foundation/Networking/GhostSession/GhostEntityTracker.cs`, `tests/EditMode/Networking/GhostSession_PromotionStateConstraints_tests.cs` | S | Low | — | 2026-07-18 | Backlog — resolve when a real Movement/Combat System story adds a position-update or attack-queue code path |
| TD-017 | Test Debt | AC-TOK-5 (Story 016, `networking-session-token.md` line 129) has two conjuncts: "a client... receives a new token" (owned and tested by `SessionTokenStore`) and "character state is preserved" (no rollback from the restart). The second conjunct has no test anywhere in this repo — correctly out of scope for `SessionTokenStore` (a pure token-store class with no character-state fields), but no other story currently claims ownership of it either. `character-persistence.md` describes a related-but-distinct scenario (live `SaveIrreversibleOutcome` failure rollback, not a post-restart full-auth reconnect flow). Needs an owner assigned — likely a future Character Persistence or session-restore integration story — before AC-TOK-5 can be considered fully closed end-to-end. | `production/epics/networking-core/story-016-session-token-nscrt.md`, `design/gdd/networking-session-token.md`, `design/gdd/character-persistence.md` | S | Medium | — | 2026-07-18 | Backlog — assign ownership when a session-restore/Character Persistence story lands |
| TD-016 | Test Debt | AC-NC-42's `ConnectionStateMachine_InFlightRpcAuthorityBoundary_Tests` (Story 015) proves that a state-check-then-apply guard pattern cannot produce a partial-application state — but the guard (`ProcessRpcIfConnected`) is a local function defined inside the test itself, not a call into any shared/reusable production method, because no real RPC-dispatch layer exists yet in this codebase. This is documentation of a required pattern, not a regression gate: it cannot catch a defect in whatever real `AllocateFreePointRequest` handler gets written later, since that handler doesn't exist yet and the test never calls into it. Must be superseded (not merely supplemented) by a new test against the real handler once a real RPC-dispatch layer implements `AllocateFreePointRequest`. | `tests/EditMode/Networking/Session_TTLExpiry_ZoneCrash_tests.cs` (`ConnectionStateMachine_InFlightRpcAuthorityBoundary_Tests`) | S | Medium | — | 2026-07-18 | Backlog — resolve when a real RPC-dispatch layer exists |
| TD-015 | Test Debt | AC-NC-35(a)'s test (`EvaluateReassemblyTimeout_LastFragmentDropped_...`, Story 015) exercises `ITransportFaultInjector.DropSnapshotFragment`/`TryConsumeSnapshotFragmentDrop` as a standalone sanity check that is never wired into `ZoneSnapshotReassemblyTracker`'s actual code path — `EvaluateReassemblyTimeout` is driven purely by elapsed ticks since `BeginReassembly` and never consults the fault injector at all. What's proven is "if mid-reassembly and enough ticks elapse, a retransmit fires and the gate stays closed" (true, useful, and the bulk of what AC-NC-35 cares about) — not "a dropped last fragment is what caused that state" (that causal link exists only in the test's comments/narrative). No real transport/snapshot-send path exists yet in this codebase for the fault injector to wire into; both unity-specialist and qa-tester code review passes agreed this is acceptable interim scope, matching this epic's forward-dependency precedent (Story 011/013), but flagged it should not be silently treated as fully closing AC-NC-35. Resolve by wiring a real fragment-receive path (or the future zone-entry orchestration class) to actually consume `TryConsumeSnapshotFragmentDrop`'s result and feed it into the tracker. | `src/Foundation/Networking/ZoneSnapshotReassembly/ZoneSnapshotReassemblyTracker.cs`, `tests/EditMode/Networking/Session_TTLExpiry_ZoneCrash_tests.cs` | S | Low | — | 2026-07-18 | Backlog — resolve when a real zone-entry orchestration story lands |
| TD-014 | Documentation Debt | TR-ID collision: Story 009 (Fixed 20Hz Server Tick Loop) and Story 011 (Commit-Before-Broadcast Generic Pattern) both cite `Requirement: TR-net-002` in their headers, but `EPIC.md`'s own placeholder table defines `TR-net-002` as "Server tick at exactly 20Hz" — Story 009's territory, not Story 011's. Underlying cause: `docs/architecture/tr-registry.yaml` has no populated entries at all (only its template header) — the same systemic gap noted in every Networking Core story since 003, now compounded by a second story independently claiming the same placeholder ID. Did not block either story (both implementations sourced their requirement directly from the GDD, not the empty registry), but must be resolved — Story 011 needs its own distinct TR-ID — before `/story-readiness` TR-ID validation can be trusted for this epic. | `production/epics/networking-core/story-009-*.md`, `story-011-*.md`, `docs/architecture/tr-registry.yaml`, `EPIC.md` | S | Medium | — | 2026-07-17 | Backlog — resolve before TR registry is populated |
| TD-013 | Architecture Debt | `SESSION_TTL_SECONDS` (default 300, tuning range [60,600]) is declared as a provisional `public const int` on `CommitBeforeBroadcastSequencer` (Story 011) — the first code reference for a value previously referenced only in GDD prose across 5+ documents ("owned by Networking Core CR-NET-2" per `character-persistence.md`). No real session-lifecycle/connection-registry code exists yet to own this constant properly; `disconnectClient`/`preserveSessionForTtl` are likewise minimal delegate seams, not a real implementation. Follow the `ServerTickLoop.TICK_RATE_HZ` precedent's resolution path once Story 012/013 (Player Connection State Machine) lands — relocate ownership to wherever the real session registry ends up living. | `src/Foundation/Networking/CommitBeforeBroadcast/CommitBeforeBroadcastSequencer.cs` | S | Low | — | 2026-07-17 | Backlog — resolve when Story 012/013 implements the real session registry |
| TD-012 | Architecture Debt | `RUBatchWriter.Write` allocates several `List<PendingSubMessage>`/array objects (`buckets`, `categoryBytes`, `evicted`, up to 7 per-category `List<T>` instances) on every call — the packet *buffers* are pre-allocated via `ZoneBufferPool`/`ClientBufferSet`, but this bookkeeping is not, contradicting the GDD's "zero GC on the hot path" rationale for the whole buffer-pooling design. At the documented 50-client/20Hz scenario this is up to ~1000 calls/sec, each producing several short-lived Gen0 objects. unity-specialist code review rated this SUGGESTION not BLOCKING (small, short-lived garbage; mechanical fix). Recommended fix: thread pre-allocated scratch collections through as parameters or cache them per-connection, mirroring the `ClientBufferSet` pattern. | `src/Foundation/Networking/WireProtocol/RUBatchWriter.cs` | M | Medium | — | 2026-07-11 | Backlog |
| TD-011 | Documentation Debt | 5 provisional wire `MessageTypeID` values are used but not formally registered anywhere except in-code doc comments: `DamageEvent` (0x0301), `GoldSyncEvent` (0x0520), `CycleTimerBroadcast` (0x0302), `EntityPositionUpdate` (0x0303) — Story 007 — and `HeartbeatMessage` (0x0210) — Story 008. ADR-004 only registers the `0x0100–0x01FF` batch-header range; the GDD explicitly defers individual application message IDs to "the Networking ADR," which does not yet enumerate them (`CycleTimerBroadcast`'s own wire-format diagram literally shows `[0x????]`; `HeartbeatMessage`'s CR-NET-7.10 schema block assigns no ID at all). DamageEvent/GoldSyncEvent reuse values already de-facto established in Story 004's doc-comment examples; Cycle/Position/Heartbeat are genuinely fresh assignments with no prior precedent. Must be resolved via a Networking ADR amendment or a dedicated MessageTypeID registry before Story 025-027 (dispatch) or any client-side decoder depends on these exact values — a later renumbering at that point would be a breaking wire change. | `src/Foundation/Networking/WireProtocol/{DamageEvent,GoldSyncEvent,CycleTimerBroadcast,EntityPositionUpdate,HeartbeatMessage}.cs` | S | Medium | — | 2026-07-11 | Backlog — resolve before Story 025-027 |
| TD-010 | Correctness Debt | `TransportFaultInjector._sequenceNumber` defaults to `0` and is never forced to `1` on construction — the very first `ConsumeNextSequenceNumber()` call (before any `SetSequenceNumber()`) returns `0`, which CR-NET-7.5 reserves as "uninitialized, must never appear in a valid message." Story 005 fixed the *wraparound* vector for this same invariant but not the *unseeded-default* vector. Not fixed now because no production send path exists yet to actually consume this value (test-harness-only concern today); flag for whichever future story wires this harness into a real send path. | `src/Foundation/Networking/TestHarness/TransportFaultInjector.cs` | S | Low | — | 2026-07-09 | Backlog |
| TD-008 | Test Debt | `WireFixedPointCodec.EncodeCritChance`/`EncodeAttackSpeedMultiplier` have no overflow guard — an out-of-range input silently wraps via the `ushort` cast rather than clamping+logging like the other four CR-NET-7.2 encoders (position, rotation, direction, cycleTimer). CR-NET-7.2 documents a valid range for these two fields but Story 003's Implementation Notes only specify a runtime guard for the other four; accepted as a deliberate scope-discipline call, not an oversight. | `src/Foundation/Networking/WireProtocol/WireFixedPointCodec.cs` | S | Low | — | 2026-07-09 | Backlog |
| TD-009 | Correctness Debt | None of `EncodePosition`/`EncodeRotation`/`EncodeDirection` guard against `NaN`/`Infinity` input — `Mathf.Clamp(NaN, ...)` returns `NaN` (the clamp body never executes since IEEE754 NaN comparisons are always false), and the subsequent `Mathf.RoundToInt` + `(short)` cast of `NaN` is not guaranteed to behave identically between the Mono Editor test run and an IL2CPP ARM64 client build. Flagged by unity-specialist code review; no AC requires NaN handling so not fixed in Story 003. | `src/Foundation/Networking/WireProtocol/WireFixedPointCodec.cs` | S | Medium | — | 2026-07-09 | Backlog |
| TD-001 | Architecture Debt | `ItemDatabase.GetItemsByCategory` returns the internal `List<ItemDefinition>` upcast to `IReadOnlyList<ItemDefinition>` rather than a defensive wrapper. A caller casting back to `List<T>` could mutate the database's internal index, undermining the "single authoritative data" guarantee. Accepted for Story 001 because it requires a deliberate invalid cast to trigger — no accidental-misuse path exists today. | `src/Foundation/ItemDatabase/ItemDatabase.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-002 | Dependency Debt | **RESOLVED 2026-07-04**: `src/` and `tests/` are now local Unity packages (`com.irongrind.src`, `com.irongrind.tests`, both `file:` references in `Packages/manifest.json`), each with their own asmdef, `InternalsVisibleTo` wiring test seams across the assembly boundary, and `"testables"` registration so the Test Runner discovers package tests. Confirmed working: all 128 EditMode tests across Character Stats (Stories 001–007) and Item Database (Stories 001–003) compile and pass in the real Editor. | `src/package.json`, `tests/package.json`, `src/Foundation/IronGrind.Foundation.asmdef`, `src/Foundation/AssemblyInfo.cs`, `tests/EditMode/IronGrind.Foundation.EditModeTests.asmdef`, `Packages/manifest.json` | — | — | — | 2026-07-04 | Closed |
| TD-003 | Test Debt | Two documented `ItemDatabase.Initialize()` behaviors have no regression test: duplicate `ItemID` across records is last-write-wins (no error logged), and `null` entries in the records enumerable are silently skipped. Neither is required by a story AC, but both are contractual behavior that a future refactor could silently change. Accepted for Story 001 because the 11 blocking ACs are fully covered; these are defensive-behavior gaps only. | `src/Foundation/ItemDatabase/ItemDatabase.cs`, `tests/EditMode/ItemDatabase/ItemDatabase_Core_tests.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-004 | Documentation Debt | `ItemDefinitionValidator`'s class remarks justify using a `HashSet<StatID>` (instead of `Enum.IsDefined`) as if `StatID` carries a unique IL2CPP stripping risk that `GearSlot` (validated via `Enum.IsDefined` two lines away) does not. Per the Unity specialist review, both enums are equally protected by their `[SerializeField]` usage elsewhere — the real justification is boxing-avoidance in general, not an asymmetric risk between the two enums. Accepted for Story 002 because the code itself is correct either way; only the comment's reasoning is imprecise. | `src/Foundation/ItemDatabase/ItemDefinitionValidator.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-005 | Test Debt | Two validator code paths have no test coverage: the undefined-`ItemCategory` default-case error (`ValidateRecord`'s `switch` default branch), and the Equipment-record-with-`ConsumableData`-set cross-contamination check in `ValidateEquipment`. Neither is required by a story-002 AC (they're defensive paths the validator happens to also cover), but both are real, reachable code paths that a future refactor could silently break. Accepted for Story 002 because all 15 blocking ACs are fully covered by the 25 existing tests. | `src/Foundation/ItemDatabase/ItemDefinitionValidator.cs`, `tests/EditMode/ItemDatabase/ItemDatabase_Validator_Error_tests.cs` | S | Low | — | 2026-07-04 | Backlog |
| TD-006 | Dependency Debt | **RESOLVED 2026-07-04**: a real Unity 6.3 LTS (6000.3.10f1) Editor project exists at the repo root; `src`/`tests` compile as local packages (TD-002); all 128 EditMode tests across Stories 001–007 (Character Stats) and 001–003 (Item Database) actually pass in the Editor — the first real execution since these stories were written. Four genuine pre-existing bugs were found and fixed along the way (all undetected until this session because nothing had ever compiled before): (1) `StatChangedHandler`/`EntityDiedHandler` used bare instead of qualified — they're nested types inside the `CharacterStats` class, not namespace-level; (2) `Object.DestroyImmediate` ambiguous between `System.Object`/`UnityEngine.Object` in 4 test files; (3) `CharacterStats.StatSlotCount` bare reference misresolved because the test namespace itself ends in "...CharacterStats", colliding with the class name; (4) `(ItemCategory)999` — compile-time constant overflow on a byte-backed enum (CS0221), inherited from the story's own illustrative AC-19 text. **Still open**: Story 004's `ItemDatabaseSeeder` menu tool has not been run yet — the 34 real `.asset` files and smoke-check evidence are still pending. | — | — | — | — | 2026-07-04 | Closed (Story 004 asset generation tracked separately, not blocking) |
| TD-007 | Documentation Debt | ~40 files across `design/gdd/`, `design/registry/entities.yaml`, and other project docs still reference lowercase `assets/...` paths for future (not-yet-authored) asset locations (e.g. `assets/data/SkillSystemConfig.asset` in `entities.yaml`). Now that the real folder is `Assets/` (capitalized), these will be wrong the moment those systems are actually implemented. Not fixed now because the referenced assets don't exist yet and the blast radius (~40 files) is large relative to the value of fixing paths with no real target yet. Fix each reference when its owning system is actually implemented, not in bulk. (Excludes `.claude/` framework files, skill/hook templates, and the `CCGS Skill Testing Framework/` sandbox, which correctly use `assets/` as an engine-agnostic generic term, not a literal path into this project.) | `design/gdd/*.md`, `design/registry/entities.yaml` (~40 files) | M | Low | — | 2026-07-04 | Backlog — fix opportunistically per-system, not in bulk |

### Rules
- Tech debt is not inherently bad — it is a tool. The register tracks conscious decisions.
- Every debt entry must explain WHY it was accepted (deadline, prototype, missing info)
- "Scan" should run at least once per sprint to catch new debt
- Items older than 3 sprints without action should either be fixed or consciously accepted with a documented reason
